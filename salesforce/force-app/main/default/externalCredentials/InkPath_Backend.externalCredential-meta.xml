<?xml version="1.0" encoding="UTF-8"?>
<!--
  External Credential for the InkPath backend.

  Authentication is a single custom header `x-api-key` carrying the backend's BACKEND_API_KEY.
  The SECRET itself cannot live in source — after deploying, open Setup → Security → Named
  Credentials → External Credentials → "InkPath Backend" → the principal below, and add a
  Custom Header named `x-api-key` whose value is your backend API key (or an authentication
  parameter referenced by that header). See salesforce/README.md.
-->
<ExternalCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>InkPath Backend</label>
    <authenticationProtocol>Custom</authenticationProtocol>
    <externalCredentialParameters>
        <parameterName>InkPathPrincipal</parameterName>
        <parameterType>NamedPrincipal</parameterType>
        <sequenceNumber>1</sequenceNumber>
    </externalCredentialParameters>
</ExternalCredential>
